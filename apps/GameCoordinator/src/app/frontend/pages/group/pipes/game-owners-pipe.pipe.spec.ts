import { GameOwnersPipe } from './game-owners-pipe.pipe';

describe('GameOwnersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GameOwnersPipe();
    expect(pipe).toBeTruthy();
  });
});
